<div class="d-flex justify-content-center align-items-start bg-dark pt-1">
  <div class="floating-card" style="width: 100%;" >


        <h1 class="display-5 mb-3 heading-gold">Menu</h1>

        <table class="table table-hover table-dark align-middle">
          <thead class="table-dark">
          </thead>

          <tbody>
            <% @menu_items.each_with_index do |menu_item, index| %>
              <tr>
                <td class="fw-semibold"><%= menu_item.name %></td>
                <td>€ <%= number_with_precision(menu_item.price, precision: 2) %></td>
                <td class="text-end">
                  <%= link_to "Details", menu_item_path(menu_item), class: "btn btn-gold btn-sm" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
  </div>
</div>
